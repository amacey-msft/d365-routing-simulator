# McManus — History

## Learnings
- Power Platform admin portals use model-driven app selectors that can change between releases
- storageState reuse significantly speeds up repeated Playwright runs
- captureStep writes both the screenshot file and updates map.json atomically
- Viewport must be 1440×900 for consistent screenshots matching lab guide
