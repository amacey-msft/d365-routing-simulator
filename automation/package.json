{
  "name": "d365-routing-automation",
  "version": "1.0.0",
  "description": "Playwright automation harness for D365 Unified Routing lab guide screenshot capture",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "capture": "npx playwright test --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "dotenv": "^16.4.0"
  }
}
